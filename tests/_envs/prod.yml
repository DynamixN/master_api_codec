# suite config
suites:
    api:
        actor: ApiTester
        path: .
        modules:
            enabled:
                - REST:
                    url: http://sceal.prod.gbksoft.net/api/v1/
                    depends: PhpBrowser